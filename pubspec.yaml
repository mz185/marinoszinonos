name: marinoszinonos
description: "Marinos Zinonos"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Arch
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5

  # UI
  google_fonts: ^6.2.1
  adaptive_theme: ^3.6.0
  font_awesome_flutter: ^10.7.0

  # Utils
  url_launcher: ^6.3.0

  # Services
  firebase_core: ^3.3.0
  cloud_functions: ^5.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
